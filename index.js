/* eslint-disable no-console */
/* eslint-disable no-unused-vars */
const express = require('express');

const app = express;

const lipu = 'hello world';

app.listen(3000, () => {
  console.log('App listening on port 3000');
});
